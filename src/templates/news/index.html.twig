{% extends 'base.html.twig' %}

{% block title %}{{ new.getTitle() }}{% endblock %}

{% block body %}
   <div class="container">
        <h3 class="m-3">{{ new.getTitle() }}</h1>
        <p>
            {{ new.getDescription() }}
        </p>
    </div>        
{% endblock %}
